branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "Direct push to $branch is not allowed. Create a branch and open a PR instead."
  exit 1
fi
